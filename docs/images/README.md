# Screenshots

Place the application screenshot here as `screenshot.png`.

To capture a screenshot:
1. Run `miniVPN.exe` from `build/build/bin/`
2. Take a screenshot of the mode selection screen
3. Save it as `screenshot.png` in this folder
